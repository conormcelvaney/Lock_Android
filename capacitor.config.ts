import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trainingroom.lockandroid',
  appName: 'Lock Android',
  webDir: 'dist'
};

export default config;
